#[test]
#[allow(non_snake_case)]
fn {name}() {{
    println!("Parsing file: {filepath}");
    let input = include_bytes!("{filepath}");
    let res = seratodj::tag::{parser}::parse_{tag_type}(&input[..]);
    if let Err(err) = res {{
        eprintln!("Error: {{:?}}", err);
        unreachable!();
    }}
}}
